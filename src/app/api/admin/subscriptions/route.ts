// src/app/api/admin/clients/broadcast/route.ts

import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({ message: 'Broadcast API endpoint placeholder.' })
}
